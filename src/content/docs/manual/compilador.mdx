---
title: Ejecutar el Compilador
sidebar:
  order: 4
---

import { Steps } from '@astrojs/starlight/components';

Actualmente, el compilador de LibreScript se ejecuta a través de la línea de comandos de Node.js.

<Steps>
  1. Guarde su Código Fuente: Escriba su programa LibreScript en el archivo `test.ls` .
      Ejemplo test.ls:
      ```ts
      //Fragmento de código
      $nombre: texto = "Mundo";
      imprimir("Hola, " + $nombre + "!");
      ```
  2. Ejecutar el Compilador: Abra una terminal o línea de comandos y ejecute el siguiente comando:
      ```bash
      node main.js
      ```
      :::note
      El compilador procesará el archivo. Si no hay errores semánticos, verá un mensaje como "Análisis semántico completado sin errores. " en la consola. Si hay errores de sintaxis o semánticos, se mostrarán mensajes de error detallados.
      :::

      ![Terminal](/AstroLibresScript/img/EjecutarCompilador/ejecutar.png)
</Steps>